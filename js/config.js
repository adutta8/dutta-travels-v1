// ===== Dutta Travels site configuration =====
// Replace the values below once you have set them up — see README.md for exact steps.

const SITE_CONFIG = {
  // Google Sheet published-as-CSV link that drives the availability calendar.
  // README.md explains how to create this: File -> Share -> Publish to web -> CSV.
  // Leave the placeholder below until you've published your own sheet —
  // the availability page will show a friendly "not connected yet" message.
  BOOKINGS_CSV_URL: "PASTE_YOUR_GOOGLE_SHEET_CSV_LINK_HERE",

  // Formspree endpoint used by the enquiry and contact forms.
  // Sign up free at https://formspree.io, create a form, and paste its
  // endpoint here, e.g. "https://formspree.io/f/abcdwxyz"
  FORMSPREE_ENDPOINT: "PASTE_YOUR_FORMSPREE_ENDPOINT_HERE",

  // Phone number used for tel: links and the WhatsApp button (digits only, with country code).
  WHATSAPP_NUMBER: "91XXXXXX8884",
  PHONE_DISPLAY: "+91 XXXXXX8884",
  EMAIL: "DTBusiness@gmail.com",
};
